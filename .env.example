# Email Configuration for Contact Form

# Titan Email SMTP (Production)
EMAIL_HOST=smtp.titan.email
EMAIL_PORT=587
EMAIL_USER=info@lakesideretreat.co.nz
EMAIL_PASS=your_titan_email_password
FROM_EMAIL=info@lakesideretreat.co.nz

# Server Configuration
NODE_ENV=production
PORT=10000

# Note: For Render deployment, Render will set its own PORT automatically
# Email credentials should be set in Render's environment variables dashboard